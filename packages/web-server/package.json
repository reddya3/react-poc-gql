{
  "name": "web-server",
  "version": "1.0.0",
  "description": "PPE web app's web server",
  "author": "Elsevier",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "nodemon src/server.js",
    "start:mock": "NODE_ENV=development nodemon src/server.js",
    "test": "jest --config=jest.config.unit.json --coverage",
    "test:watch": "jest --config=jest.config.unit.json --watch"
  },
  "dependencies": {
    "@newrelic/native-metrics": "^3.0.0",
    "compression": "^1.7.2",
    "express": "^4.16.3",
    "helmet": "^3.12.0",
    "http-proxy-middleware": "^0.18.0",
    "newrelic": "^7.0.0",
    "winston": "^2.4.1"
  },
  "devDependencies": {
    "jest": "^22.4.3",
    "nodemon": "^1.18.7",
    "supertest": "^3.0.0"
  }
}
